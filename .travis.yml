addons:
  artifacts:
    debug: true
    paths:
    - libs.zip
    - build.zip
    - testbuild.zip
    target_paths: 6c3b0aa4-4c5e-466f-8007-3ccf1d55d3ce
after_success:
- ./df-build.sh
before_install:
- sed -i.bak -e 's|https://nexus.codehaus.org/snapshots/|https://oss.sonatype.org/content/repositories/codehaus-snapshots/|g'
  $HOME/.m2/settings.xml
cache:
  directories:
  - $HOME/.m2
install: mvn install -q -DskipTests=true
jdk:
- oraclejdk8
language: java
script: mvn test -q
services:
- mongodb
- riak
sudo: false
# Modified by Insights Service at 2016-06-15 12:26:03.556577
